# Suspending part-way through writing partition tables and LUKS headers is how a
# machine ends up with neither the old system nor the new one.
[Login]
HandleSuspendKey=ignore
HandleHibernateKey=ignore
HandleLidSwitch=ignore
HandleLidSwitchExternalPower=ignore
