# A fresh machine-id per install.
#
# This matters more here than on an ordinary distribution: two volumes on one
# machine must look like two machines, and a shared machine-id is exactly the
# kind of cross-volume metadata the deniability design cannot afford.
---
systemd: true
dbus: true
dbus-symlink: true
entropy-copy: false
