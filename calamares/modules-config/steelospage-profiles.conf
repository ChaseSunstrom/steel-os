# SteelOS installer page: Profiles
#
# A steelospage instance. The QML lives in the branding directory rather than in a
# compiled-in resource, so the page and the palette it uses ship together and
# can be replaced together.

---
qmlSearch: branding
qmlFilename: steelos-profiles
qmlLabel:
    name: "Profiles"
