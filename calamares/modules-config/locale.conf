---
region:  "Europe"
zone:    "London"

# No geoip block at all. A GeoIP lookup would mean the installer makes a network
# request reporting this machine's address to a third party before the user has
# chosen a DNS provider or seen the firewall. The timezone is picked by hand on
# this page, which costs one click.
